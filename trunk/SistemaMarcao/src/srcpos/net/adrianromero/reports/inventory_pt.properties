# Sample ResourceBundle properties file

label.title=Warehouses

label.ref=Ref.

label.name=Name

label.sec=Security

label.units=Units

label.valuebuy=Value buy

label.valuesell=Value sell

label.valuevolume=Volume

label.valuecost=Cost

label.max=Maximum
